<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
                name="logic"
                type="com.kpiroom.bubble.ui.main.fragments.profile.ProfileLogic"/>

    </data>

    <com.kpiroom.bubble.util.view.ProgressLayout
            app:progressState="@{logic.progress}"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

        <androidx.coordinatorlayout.widget.CoordinatorLayout
                android:id="@+id/coordinator"
                android:layout_width="match_parent"
                android:layout_height="match_parent">

            <com.dichotome.profilebar.ui.profileBar.ProfileBar
                    android:id="@+id/profileBar"
                    android:layout_width="match_parent"
                    android:layout_height="300dp"
                    app:title="@{logic.username}"
                    app:subtitle="@{logic.joinedOn}"
                    app:photoUri="@{logic.photoUri}"
                    app:wallpaperUri="@{logic.wallpaperUri}"
                    app:onPhotoChanged="@{logic::onPhotoChanged}"
                    app:onWallpaperChanged="@{logic::onWallpaperChanged}"
                    app:onUsernameChanged="@{logic::onUsernameChanged}"
                    app:onLoggedOut="@{logic::onLoggedOut}"/>

            <com.dichotome.profilebar.ui.tabPager.TabPager
                    android:id="@+id/profilePager"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    app:fragments="@{logic.pagerFragments}"
                    app:layout_behavior="@string/appbar_scrolling_view_behavior"/>

        </androidx.coordinatorlayout.widget.CoordinatorLayout>

    </com.kpiroom.bubble.util.view.ProgressLayout>
</layout>